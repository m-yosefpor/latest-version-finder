# latest-version-finder

`source ~/token`

`./find-version`

`vimdiff latest-versions project-list`

`vimdiff latest-versions latest-versions-prev`



-TODO add a cronjob to change the later everyday and notify if there is a change
